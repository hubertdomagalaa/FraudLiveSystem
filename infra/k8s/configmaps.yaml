apiVersion: v1
kind: ConfigMap
metadata:
  name: fraud-platform-config
  namespace: fraud-platform
data:
  LOG_LEVEL: "INFO"
  ENVIRONMENT: "dev"
  QUEUE_BACKEND: "noop"
  CONTEXT_AGENT_URL: "http://agent-context"
  RISK_AGENT_URL: "http://agent-risk-ml"
  POLICY_AGENT_URL: "http://agent-policy"
  LLM_AGENT_URL: "http://agent-llm-explainer"
  DEFAULT_DECISION: "REVIEW"
