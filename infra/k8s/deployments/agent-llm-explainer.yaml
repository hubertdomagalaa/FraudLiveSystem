apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent-llm-explainer
  namespace: fraud-platform
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app: agent-llm-explainer
  template:
    metadata:
      labels:
        app: agent-llm-explainer
    spec:
      containers:
        - name: agent-llm-explainer
          image: fraud/agent-llm-explainer:latest
          ports:
            - containerPort: 8000
          env:
            - name: SERVICE_NAME
              value: agent-llm-explainer
            - name: CONSUMER_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
          envFrom:
            - configMapRef:
                name: fraud-platform-config
            - secretRef:
                name: fraud-platform-secrets
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 20
            periodSeconds: 20
